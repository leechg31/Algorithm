#include<iostream>
#include<vector>
#include<algorithm>
#include<cstdio>
#include<stack>
#include<string>

using namespace std;

stack<int> st;

int main()
{
	std::ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	while (1)
	{
		string cmd;
		cin >> cmd;

		if (cmd == "push")
		{
			int num;
			cin >> num;
			st.push(num);
		}

		else if (cmd == "pop")
		{
			cout << (st.empty() ? -1 : st.top()) << "\n";
			if (!st.empty())
				st.pop();
		}

		else if (cmd == "top")
			cout << (st.empty() ? -1 : st.top()) << "\n";

		else if (cmd == "size")
			cout << st.size() << "\n";

		else if (cmd == "empty")
			cout << st.empty() << "\n";
	}
}
